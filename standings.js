export function sortedStandings(league,conference=null){return league.teams.filter(t=>!conference||t.conference===conference).slice().sort((a,b)=>(b.wins-a.wins)||((b.pointsFor-b.pointsAgainst)-(a.pointsFor-a.pointsAgainst)));}
export function recordGame(league,game,homeScore,awayScore){if(game.played)return;game.played=true;game.homeScore=homeScore;game.awayScore=awayScore;const h=league.teams.find(t=>t.id===game.homeTeamId),a=league.teams.find(t=>t.id===game.awayTeamId);h.pointsFor+=homeScore;h.pointsAgainst+=awayScore;a.pointsFor+=awayScore;a.pointsAgainst+=homeScore;if(homeScore>awayScore){h.wins++;a.losses++;}else if(awayScore>homeScore){a.wins++;h.losses++;}else{h.ties++;a.ties++;}}
